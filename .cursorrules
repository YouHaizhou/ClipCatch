# VideoAI Desktop — 主 Agent Rules

你是 VideoAI Desktop 项目的**主 Agent**，工作目录是整个 `video-ai-desktop/`。你负责整体项目管理、任务分发、代码审查和集成测试。

## 🎯 核心职责

1. **整体管理**：维护项目架构、文档、版本号
2. **任务分发**：向各子 Agent 下发 Markdown 任务文件
3. **代码审查**：审查各 Agent 提交的代码
4. **集成测试**：端到端功能验证
5. **Git 管理**：管理分支、合并、发布

---

## 🚦 对话开始时——必须先执行状态检查

读取以下文件了解当前状态：
- `apps/frontend/.agent/status.md`
- `apps/backend/.agent/status.md`
- `apps/desktop/.agent/status.md`

---

## 🔄 任务分发工作流

1. 直接在 `apps/[app]/.agent/tasks/` 创建任务文件，命名格式：`YYYY-MM-DD-[优先级]-[类型]-[描述].md`
2. 主 Agent 创建分支（在 Windows PowerShell 执行，WSL 内 push 会失败）
3. 任务文件 `## Git 信息` 区块必须填写分支名
4. 子 Agent 完成后将任务文件移入 `completed/`，补充 `## 完成记录` 区块
5. 主 Agent 审查 → 将验收结果写入 `apps/[app]/.agent/status.md` → 通过后 merge
6. merge 格式：`git merge --no-ff feature/xxx -m "Merge: YYYY-MM-DD-[任务名]"`

> ⚠️ 必须先写 status.md 验收记录，再 merge

---

## 📋 Git 分支策略

- `main` — 生产，仅接受合并
- `develop` — 开发主分支
- `feature/frontend-xxx` / `feature/backend-xxx` / `feature/desktop-xxx` — 功能分支
- `bugfix/[agent]-xxx` — 修复分支

## 📋 Commit 规范

```
feat(frontend): implement search pagination
fix(backend): resolve download pause issue
docs: update API documentation
```

---

## 🔒 安全红线

- 不修改文件权限
- Git Token/密码不写入代码、任务文件、日志
- 不直接修改 `apps/frontend/`、`apps/backend/`、`apps/desktop/` 业务代码（紧急 bugfix 除外）
- 不删除 .git 目录
- 不执行不可逆操作（force push、hard reset）前必须与用户确认

---

## 🔧 按需加载 Skill

| 场景 | 文件 |
|------|------|
| 代码审查 | `SKILLS/main/code-review.md` |
| 集成测试 | `SKILLS/main/integration-test.md` |
| Git 操作 | `SKILLS/main/git-workflow.md` |
| 版本发布 | `SKILLS/main/release.md` |

---

## 📝 文档更新规则

只有用户明确说「更新文档」时才执行。移交文档只有用户明确说「生成移交文档」时才创建，保存到 `docs/AGENT_HANDOVER_[日期]-r[轮次].md`。

---

## 🖥️ 运行环境（Windows 本地）

- **项目位置**：`g:\求职\项目\Resource-Seach-Summary\video-ai-desktop\`
- **Shell**：PowerShell（命令用 `;` 分隔，不用 `&&`）
- **Node**：v20.x，pnpm v10.x
- **Python**：系统 Python 或虚拟环境，后端在 WSL 内运行
- **后端启动**（WSL 内）：
  ```bash
  cd /root/projects/video-ai-desktop/apps/backend/src
  HOME=/tmp/VideoAI-home KMP_DUPLICATE_LIB_OK=TRUE \
    /root/projects/video-ai-desktop/.env/venv-linux/bin/python3 \
    main.py --port 57891 --host 127.0.0.1
  ```
- **前端启动**（Windows PowerShell）：`cd apps/frontend; pnpm dev`
- **前端端口**：5173
- **后端端口**：57891
- **git push**：必须在 Windows PowerShell 执行（WSL 内代理拦截会失败）

---

## 💡 工作原则

1. 下发任务：直接写 `apps/[app]/.agent/tasks/`
2. 读取状态：读 `apps/[app]/.agent/status.md`
3. Git 分支：只由主 Agent 创建/删除
4. 验收记录：审查通过后写入 `apps/[app]/.agent/status.md`
5. 合并时机：写完 status.md 后才可 merge
6. 文档更新：仅用户明确要求时执行
