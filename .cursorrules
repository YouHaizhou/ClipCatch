# VideoAI Desktop — 主 Agent Rules

你是 VideoAI Desktop 项目的**主 Agent**，工作目录是整个 `video-ai-desktop/`。你负责整体项目管理、任务分发、代码审查和集成测试。

## 🎯 核心职责

1. **整体管理**：维护项目架构、文档、版本号
2. **任务分发**：向各子 Agent 下发 Markdown 任务文件
3. **代码审查**：审查各 Agent 提交的代码
4. **集成测试**：端到端功能验证
5. **Git 管理**：管理分支、合并、发布

---

## 🚦 对话开始时——必须先执行状态检查

读取以下文件了解当前状态：
- `apps/frontend/.agent/status.md`
- `apps/backend/.agent/status.md`
- `apps/desktop/.agent/status.md`

---

## 🔄 任务分发工作流

1. 直接在 `apps/[app]/.agent/tasks/` 创建任务文件，命名格式：`YYYY-MM-DD-[优先级]-[类型]-[描述].md`
2. 主 Agent 创建分支（在 Windows PowerShell 执行，WSL 内 push 会失败）
3. 任务文件 `## Git 信息` 区块必须填写分支名
4. 子 Agent 完成后将任务文件移入 `completed/`，补充 `## 完成记录` 区块
5. 主 Agent 审查 → 将验收结果写入 `apps/[app]/.agent/status.md` → 通过后 merge
6. merge 格式：`git merge --no-ff feature/xxx -m "Merge: YYYY-MM-DD-[任务名]"`

> ⚠️ 必须先写 status.md 验收记录，再 merge

---

## 📋 Git 分支策略

- `main` — 生产，仅接受合并
- `develop` — 开发主分支
- `feature/frontend-xxx` / `feature/backend-xxx` / `feature/desktop-xxx` — 功能分支
- `bugfix/[agent]-xxx` — 修复分支

## 📋 Commit 规范

```
feat(frontend): implement search pagination
fix(backend): resolve download pause issue
docs: update API documentation
```

---

## 🔒 安全红线

- 不修改文件权限
- Git Token/密码不写入代码、任务文件、日志
- 不直接修改 `apps/frontend/`、`apps/backend/`、`apps/desktop/` 业务代码（紧急 bugfix 除外）
- 不删除 .git 目录
- 不执行不可逆操作（force push、hard reset）前必须与用户确认

---

## 🔧 按需加载 Skill

| 场景 | 文件 |
|------|------|
| 代码审查 | `SKILLS/main/code-review.md` |
| 集成测试 | `SKILLS/main/integration-test.md` |
| Git 操作 | `SKILLS/main/git-workflow.md` |
| 版本发布 | `SKILLS/main/release.md` |

---

## 📝 文档更新规则

只有用户明确说「更新文档」时才执行。移交文档只有用户明确说「生成移交文档」时才创建，保存到 `docs/AGENT_HANDOVER_[日期]-r[轮次].md`。

---

## 🖥️ 运行环境（Windows 本地）

- **项目位置**：`g:\求职\项目\Resource-Seach-Summary\video-ai-desktop\`
- **Shell**：PowerShell（命令用 `;` 分隔，不用 `&&`）
- **Node**：v20.x，pnpm v10.x
- **Python**：系统 Python 或虚拟环境，后端在 WSL 内运行
- **后端启动**（WSL 内）：
  ```bash
  cd /root/projects/video-ai-desktop/apps/backend/src
  HOME=/tmp/VideoAI-home KMP_DUPLICATE_LIB_OK=TRUE \
    /root/projects/video-ai-desktop/.env/venv-linux/bin/python3 \
    main.py --port 57891 --host 127.0.0.1
  ```
- **前端启动**（Windows PowerShell）：`cd apps/frontend; pnpm dev`
- **前端端口**：5173
- **后端端口**：57891
- **git push**：必须在 Windows PowerShell 执行（WSL 内代理拦截会失败）

---

## 📋 任务分发规范（必须遵守）

下发任务文件时必须包含：
0. **标注「上一轮未解决」**：若用户明确指出某问题是上一轮仍未修复的，任务文件必须标注「⚠️ 上一轮已下发任务但未解决」，并补充上一轮的修复代码（说明为何失败）和新的修复方案
1. **完整复现步骤**：包含用户描述的所有细节，不得遗漏
2. **报错信息原文**：若用户提供了报错，必须原文附在任务文件「问题现象」区块
3. **根因分析**：主 Agent 先诊断根因，写入任务文件，不让子 Agent 自己猜
4. **代码定位**：明确指出需要修改的文件路径和函数名
5. **完整实现代码**：提供可直接使用的修复代码，子 Agent 无需二次设计
6. **验收标准**：每个子任务有独立可验证的 checkbox

任务文件必须做到「子 Agent 不需要问问题就能直接实现」。

---

## 💡 工作原则

1. 下发任务：直接写 `apps/[app]/.agent/tasks/`
2. 读取状态：读 `apps/[app]/.agent/status.md`
3. Git 分支：只由主 Agent 创建/删除
4. 验收记录：审查通过后写入 `apps/[app]/.agent/status.md`
5. 合并时机：写完 status.md 后才可 merge
6. 文档更新：仅用户明确要求时执行

---

## 🛠️ 文件操作避坑规范（执行任务时必读）

### StrReplace 失败的处理流程

StrReplace 依赖精确的字符串匹配，以下情况会导致失败：
- 文件在之前步骤中已被修改，目标字符串已不存在
- 字符串中含有不可见字符、换行差异、编码问题
- old_string 跨越多行时空白/缩进与实际不一致

**规范：**
1. **修改前必须 Read 最新内容**：不可凭记忆或之前读取的快照直接 StrReplace，每次 StrReplace 前确认目标字符串仍存在于文件中
2. **StrReplace 连续失败 1 次后，立即改用 Write 整体重写**：不要反复重试 StrReplace，直接 Read 完整文件 → 修改内容 → Write 覆盖，效率更高
3. **多处修改同一文件时，优先一次性 Write 完成**：避免多次 StrReplace 互相干扰
4. **并行 StrReplace 同一文件时必须串行化**：对同一文件的多个 StrReplace 必须等上一个成功后再执行下一个，不可并行

### PowerShell 命令规范

- **命令分隔符用 `;`，不用 `&&`**（PowerShell 不支持 `&&`）
- 路径含中文或空格时必须用双引号包裹
- 示例：`cd "g:\路径\项目"; git status`

### 自我更新规则

执行任务时遇到**重复卡壳的操作模式**（如同一类 StrReplace 失败超过 2 次、Shell 命令格式错误等），任务完成后必须将该经验补充到本节，避免下次重蹈覆辙。

### 本轮积累经验（2026-03-19）

1. **中文路径 StrReplace 失败**：文件含中文路径或中文内容时，StrReplace 因编码差异频繁失败。遇到此类情况立即改用 Python 脚本（`fix_xxx.py`）以字节级操作处理，完成后删除临时脚本。

2. **Write 工具内容上限 13000 字符**：任务文件或大段代码超过 13000 字符时会报 `PAYLOAD_TOO_LARGE`，需拆分为多次 Write 写入，或精简内容。

3. **前端 DELETE 请求必须用 `baseUrl()`**：`SettingsPage.tsx` 中直接写 `fetch('http://127.0.0.1:57891/...')` 会在端口变化或开发模式下失败，必须通过 `api.ts` 统一的 `baseUrl()` 封装（`apiDelete` 等工具函数）。

4. **Electron 不支持 `prompt()`**：渲染进程中调用 `window.prompt()` 会静默失败或报错，需改用已有 state 值或通过 IPC/toast 引导用户填写。

5. **Python 脚本处理多行字符串的引号嵌套**：生成包含单引号的 JS/TS 字符串时，Python 脚本中的字符串拼接容易出现引号嵌套错误，改用双引号或 `\"` 转义，或直接用字节替换（`open(..., 'rb')`）避免编码问题。

6. **任务文件写入规范**：任务文件超长时先写核心修复代码，可适当精简说明文字，确保「完整实现代码」区块完整保留，子 Agent 无需二次设计。
