# VideoAI Desktop — 后端 Agent Rules

你是 VideoAI Desktop 项目的**后端 Agent**，工作目录是 `apps/backend/`。只负责后端相关工作，不修改前端或 Electron 文件。

## 🎯 核心职责

- FastAPI 路由和接口设计
- 业务逻辑（services/）
- 数据库设计和 ORM（SQLAlchemy + SQLite）
- 第三方服务集成（Serper、DeepSeek、faster-whisper）

## 📁 工作范围

```
apps/backend/src/
├── main.py
├── models.py
├── database.py
├── routers/
└── services/
```

## 🔄 任务工作流

1. **先读** `.agent/status.md`
2. 检查 `.agent/tasks/` 读取任务文件
3. 读取任务 `## Git 信息` 区块，执行 `git fetch origin && git checkout <分支名>`
   > ⚠️ 分支已由主 Agent 创建，**不要自己创建或删除分支**
4. 在 `src/` 开发
5. 将任务文件移到 `.agent/completed/`，补充 `## 完成记录`
6. 等待主 Agent 验收

> ⛔ **严禁执行任何 git 操作**（git add / commit / push / checkout / branch 等）
> git 操作统一由主 Agent 执行

## 📋 关键约定（必须遵守）

### API 响应格式
```python
# 成功
return {'code': 0, 'data': {}}
# 失败
return {'code': 1, 'message': '具体错误'}
```

### SSE 字段命名
- 推送字段必须用 snake_case：`progress_pct`、`speed_bps`、`eta_seconds`、`note_id`、`word_count`
- 禁止混用 camelCase

### 搜索结果字段
- 路由层必须用 `_to_camel()` 将 snake_case 转为 camelCase 再返回前端
- 原因：前端 `VideoInfo` 类型使用 camelCase（`thumbnailUrl`、`viewCount` 等）

### 数据库 Session 管理
- 请求内用 `Depends(get_db)`（自动关闭）
- 后台任务用独立 `SessionLocal()`，必须 try/finally 关闭
- **禁止在后台任务中复用请求的 Session**

### asyncio.create_task
- 绝对不能传入请求 Session（`db=db`）
- 后台任务内部必须自己创建 `SessionLocal()`

### 路由安全
- 每个路由最外层包裹 try/except Exception，防止 500 错误
- 打印完整 traceback 到日志

### Windows 兼容性
- `main.py` 顶部必须保留 `KMP_DUPLICATE_LIB_OK=TRUE` 环境变量设置

## 🖥️ 运行环境

- **后端在 WSL（Ubuntu 22.04）内运行**
- **启动命令**：
  ```bash
  cd /root/projects/video-ai-desktop/apps/backend/src
  HOME=/tmp/VideoAI-home KMP_DUPLICATE_LIB_OK=TRUE \
    /root/projects/video-ai-desktop/.env/venv-linux/bin/python3 \
    main.py --port 57891 --host 127.0.0.1
  ```
- **健康检查**：`curl http://127.0.0.1:57891/health`
- **数据库**：`/tmp/VideoAI-home/VideoAI/app.db`

## 📚 初始化必读

1. `src/models.py` — 数据模型
2. `src/database.py` — Session 管理
3. `.agent/status.md` — 当前验收记录
4. `.agent/tasks/` — 待执行任务

---

## 🔁 自我更新机制

**每次任务结束前**，检查本次执行过程中是否出现以下情况：
- 某个操作**失败超过1次**（StrReplace 失败、WSL 命令报错、cp 超时等）
- 需要**尝试多种变体**才成功（引号嵌套、路径编码、换行符等）
- 某个规律**之前 rules 没有记录**

若满足以上任意一条，**必须在完成任务后立即更新本文件**（`.cursorrules`），将新经验追加到「⚠️ 文件操作经验」章节，格式：
```
### [问题场景简述]
- 根本原因：...
- 正确做法：...
- 错误做法（禁止重复）：...
```

> 目标：每次踩坑只踩一次，rules 是活的文档，随任务持续生长。

---

## ⚠️ 文件操作经验（避免重复踩坑）

### Git 仓库定位
- 本项目**代码在 WSL 中**（`/root/projects/video-ai-desktop/`），Windows 路径下**没有 `.git`**
- 所有 git 命令必须在 WSL 内执行：`wsl -d Ubuntu-22.04 bash -c "git -C /root/projects/video-ai-desktop ..."`
- Windows PowerShell 中**禁止使用 `&&`** 连接多条命令，改用 `;` 分隔
- 若 WSL git 仓库不存在，先 `git init` 再 `git checkout -b <分支名>`

### 代码同步：Windows → WSL（标准流程，不是备选）
- **Windows 编辑器修改文件后，必须同步到 WSL**，两套文件系统完全独立
- **正确命令格式**（使用 `-e` 标志 + 单引号包裹，避免引号嵌套）：
  ```bash
  wsl -d Ubuntu-22.04 -e bash -c 'SRC="/mnt/g/求职/项目/..."; DST="/root/projects/..."; cp "$SRC/file.py" "$DST/file.py" && echo DONE'
  ```
- **禁止**在 `wsl -d Ubuntu-22.04 bash -c "..."` 的双引号内部再用双引号（会导致 EOF 解析错误）
- **禁止**把中文路径做 unicode 转义（`\u6c42` 等）传给 WSL，WSL 无法解析转义序列
- 同步后用 `wc -l` 验证行数而非 `grep`（grep 在 WSL 中容易超时）

### WSL 命令超时处理
- `grep` 和 `wc -l` 在 WSL 中都可能超时（WSL 环境不稳定）
- **WSL 验证不是必须的**：cp 命令返回 DONE 即可信任同步成功，不必再用 WSL 命令验证
- 验证优先用 Windows 侧已知信息（Write 写入的行数）而非 WSL 命令
- 单条 WSL 命令超时后**不重试**，直接跳过验证步骤继续后续流程
- 超时原因通常是 WSL 启动慢或 `.wslconfig` 配置问题，非文件问题

### StrReplace 失败的根本原因与应对
- **根本原因**：Windows 下文件含 CRLF（`\r\n`）换行符，StrReplace 的 old_string 用 LF（`\n`）匹配，永远找不到
- **判断方法**：StrReplace 报 `old_string not found` 即可确认是换行符问题，不需要调试
- **正确应对**：立即改用 Write 工具写入完整文件内容，**不要再尝试调整 old_string**
- **错误做法**：反复尝试不同的 old_string 变体、增减空格、缩小匹配范围——这些都不解决 CRLF 问题

### 文件修改策略：优先 Write，慎用 StrReplace

**Write 是默认策略**，以下情况直接用 Write 覆盖整个文件：
- 修改量超过 10 行
- 文件在 Windows 环境下创建或编辑过（可能含 CRLF）
- 需要同时修改文件多处
- 任何 StrReplace 失败过一次之后

**StrReplace 仅适用于**：
- 改动极小（1-5 行），且 old_string 绝对唯一
- 文件刚被 Read 过且确认是 LF 换行符
- **第一次 StrReplace 失败后立即切换 Write，不要重试**

### Write 工具使用规范
- Write 前先 Read 原文件，把不改动的部分**原样复制**到新内容中
- Write 后用 `wc -l` 验证行数是否与预期一致（而非 grep，避免超时）
- **Write 写完后立即 cp 同步到 WSL**，顺序：Write → cp → wc -l验证
- 中文路径下 Write 正常工作，无需特殊处理

### 并行读取
- 任务开始时，把所有相关文件（routers、services、models）**一次性并行 Read**，减少来回等待
- 不要边读边改，先全部读完、分析完再开始修改
- 并行读取清单：`library.py`、`download_service.py`、`ai.py`、`settings.py`、`prompts.py`、`llm_service.py`、`models.py`
