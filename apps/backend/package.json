{
  "name": "video-ai-backend",
  "version": "1.0.0",
  "description": "VideoAI Backend - FastAPI",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn src.main:app --reload --host 127.0.0.1 --port 57891",
    "start": "python -m uvicorn src.main:app --host 127.0.0.1 --port 57891",
    "seed": "python src/seed.py",
    "check-db": "python scripts/check_db.py",
    "check-keys": "python scripts/check_keys.py"
  }
}
