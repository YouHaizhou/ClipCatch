# 2026-03-18-P1-bugfix-下载与搜索后端修复

## Git 信息
- **工作分支**：`feature/backend-api-v2`

> ⛔ **严禁执行任何 git 操作**

---

## 问题 2：Twitter 搜索

**已在上次任务（2026-03-18-P1-bugfix-搜索功能修复）中修复**，`_search_twitter` 已改用 `/videos` 接口优先 + `/search` 降级，本次无需改动。

---

## 问题 3：YouTube 下载「链接解析失败」修复

### `src/services/download_service.py`
- `extract_video_info` 增加 `http_headers` 模拟浏览器 UA
- 增加 `extractor_args: youtube: player_client: [web, android]` 应对 YouTube 新防护
- 增加 `ffmpeg_location` 注入
- 异常包装为 `ValueError` 并给出明确提示
- `start_download` 的 `ydl_opts` 同步增加 UA 和 extractor_args

### `src/routers/download.py`
- 新增 `_friendly_download_error()` 函数，将 yt-dlp 原始错误转为中文提示
- 覆盖：登录要求 / 私密视频 / 地区限制 / 版权 / 已删除 / 通用错误

### `requirements.txt`
- `yt-dlp==2024.3.10` → `yt-dlp>=2024.11.4`

---

## 验收标准

- [x] 挂梯子后，YouTube 视频链接能正常解析并开始下载
- [x] 解析失败时给出具体原因（登录/私密/不可用）而非通用错误
- [x] yt-dlp 版本更新到 2024.11.4+
- [x] Twitter 搜索已在上次修复，配置 Serper Key 后能返回结果

---

## 完成记录

- **完成时间**：2026-03-18
- **实际工时**：约 0.5h
- **修改文件**：
  - `src/services/download_service.py`：extract_video_info 增强，start_download 补充 UA/extractor_args
  - `src/routers/download.py`：新增 _friendly_download_error，优化错误提示
  - `requirements.txt`：yt-dlp 升级到 >=2024.11.4
- **未修改**：`search_service.py`（Twitter 上次已修复）
